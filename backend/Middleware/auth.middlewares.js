// use jwt token here to check every user endpoint access
// this is not priority, do not focus on this as it may cause trouble of testing for core features

function checkAuth(token) {
    //
}

module.exports = checkAuth;